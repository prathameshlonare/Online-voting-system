# ==========================================
# BACKEND - Production (Simplified)
# ==========================================
FROM node:22-alpine
WORKDIR /app
ENV NODE_ENV=production

# Copy mock source files directly
COPY src/mocks/ ./src/mocks/

# Copy HTTP server wrapper
COPY docker/server.js ./server.js

EXPOSE 4000
CMD ["node", "server.js"]
